drop trigger territory_change_log on territory;
drop trigger territory_validation on territory;

drop trigger congregation_boundary_change_log on congregation_boundary;
drop trigger congregation_boundary_validation on congregation_boundary;

drop trigger subregion_change_log on subregion;
drop trigger subregion_validation on subregion;

drop trigger card_minimap_viewport_change_log on card_minimap_viewport;
drop trigger card_minimap_viewport_validation on card_minimap_viewport;
